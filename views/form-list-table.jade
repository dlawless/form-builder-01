table.table.table-striped.table-condensed
  thead
    tr
      th Name
      th Display Name
  tbody
    tr(ng-repeat='form in forms')
      td {{form.name}}
      td {{form.displayName}}
      td
        a(href='form/details/{{form._id}}') Details